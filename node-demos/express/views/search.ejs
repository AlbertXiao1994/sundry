<h1>Search Results for <%= search %></h1>

<% if (results.length) { %>
  <ul>
    <% for (var i = 0; i < results.length; i++) { %>
      <li>
        <a href="<%= results[i].url %>"><%= results[i].name %></a> <%= results[i].star %>
      </li>
    <% } %>
  </ul>
<% } else { %>
  <p>no result</p>
<% } %>